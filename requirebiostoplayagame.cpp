#include "require bios to play a game"

void start_game() {
    // Load the game's ROM into memory
    // ...

    // Start the game's main loop
    while (true) {
        // Update game logic
        // Render the game's screen
        // Check for user input
    }
}

int main() {
    // Require the BIOS to play the game
    require_bios_to_play_game();
    // Start the game
    start_game();
    return 0;
}